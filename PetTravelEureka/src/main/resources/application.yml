spring:
  application:
    name: eureka

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    
server:
  port: 8761

logging:
  level: 
    com:
      netflix:
        eureka: ALL 